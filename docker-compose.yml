version: '3.3'

services: 
  web_server:
    depends_on: 
      - image_extraction_server
    image: nginx:1.18-alpine
    container_name: web-server
    restart: always
    ports: 
      - 8080:80
    volumes:
      - ./config:/etc/nginx/conf.d
      - ../hkcic-web/build:/var/www/hkcic-web
      - www-data:/var/www/report-file
    
  image_extraction_server:
    build: .
    container_name: image-extraction-server
    restart: always
    volumes: 
      - www-data:/var/www/report-file
      
volumes: 
  www-data:

